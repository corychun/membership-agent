Manual smoke test checklist:
1. GET /health returns 200
2. POST /quote with chatgpt-business-monthly returns quote JSON
3. POST /orders returns order_id
4. POST /payments/mock-checkout returns checkout_url
5. POST /webhooks/mock-payment with status=paid updates order
6. POST /deliveries/{order_id}/complete marks order fulfilled
